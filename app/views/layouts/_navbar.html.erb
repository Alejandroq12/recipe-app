<% if user_signed_in? %>
<nav class="navbar navbar-expand-md navbar-light bg-dark py-2">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>"><%= image_tag('logoo.png', alt: 'Logo') %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link nav-link-large <%= 'fw-bold p-2 rounded text-danger shadow-lg active-link' if current_page?(recipes_path) %> <%= 'text-light' unless current_page?(recipes_path) %>" href="<%= recipes_path %>">Recipe List</a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-link-large <%= 'fw-bold p-2 rounded text-danger shadow-lg active-link' if current_page?(public_recipes_path) %> <%= 'text-light' unless current_page?(public_recipes_path) %>" href="<%= public_recipes_path %>">Public Recipes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-link-large <%= 'fw-bold p-2 rounded text-danger shadow-lg active-link' if current_page?(foods_path) %> <%= 'text-light' unless current_page?(foods_path) %>" href="<%= foods_path %>">Food List</a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-link-large <%= 'fw-bold p-2 rounded text-danger shadow-lg active-link' if current_page?(new_food_path) %> <%= 'text-light' unless current_page?(new_food_path) %>" href="<%= new_food_path %>">New Food</a>
        </li>
        <li class="nav-item">
          <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary nav-link-large text-light", form_class: "d-inline" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
<% end %>
